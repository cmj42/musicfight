# 体感节奏 RPG — 游戏设计 & 实现计划

> **工作标题(Working Title):** 待定
> **类型:** 体感(摄像头)节奏拳击 + RPG
> **平台:** 桌面端(Windows / Mac),Electron 打包成 .exe / .app
> **美术:** 赛博朋克 · 扁平 / 剪影 / 霓虹
> **当前状态:** 核心体感检测(上中下 × 左右手出拳)+ MIDI 实时合成 + 技能系统 已跑通;主菜单 / 双语 / 设置 / 存档 已上线。**本阶段:搭「序章」的剧情过场 + 章节→关卡结构 + 训练模式 + 抱臂暂停。**(对话台词集中在可编辑的 `story.js`;关卡数据在 `chapters.js`。)

---

## 0. 一句话主题(整个项目的指南针)

> **在一个把你当作「待删除旧版本」的系统里,坚持「我是谁、我爱谁」,本身就是胜利。**

所有取舍——剧情、机制、美术、结局——都回到这一句。你不炸塔、不拯救世界;你只是拒绝被删除,并带上你在乎的那一个逃出去。小、私人、却很硬的胜利。

---

## 1. 创意设计

### 1.1 设定与世界观

- 赛博朋克都市。主角与伙伴在宏大世界里都只是 **nobody**。
- 大背景里真正的「英雄主角」可能正在努力炸荒坂塔式的巨头——也可能像强尼·银手那一次,炸完了**也无事发生**。
- 我们的主角不参与这场宏大叙事。她只是**趁公司被袭击、算力被调走的混乱窗口**,完成自己的事:找回自我 + 伙伴,跑路。
- 命题:在个体什么都改变不了的庞大系统里,**个体的坚持**意味着什么。

**世界设定:员工 = 多个上传副本(本作的阶级土壤)。**
- 这个世界的雇佣合同含一条:把你自己的**副本上传给公司**,与本人**并行**处理多任务;副本定期把记忆**同步回本人**。作为「数字副本」有一些基本权益(如不受折磨)。
- 上传遍地、副本是常规劳动力——所以主角的可怕之处**不是「她是个副本」**(人人都是),而是她是**系统归不了类的那一个**。
- 因为偷盗公司财产并造成破坏,工程师的副本**被抵扣给公司一段时间作为补偿 = 强制劳动(打白工)**。主角的处境因此是:**作为抵债资产,她是公司财产;作为可能可删的模型,她待清理**——既是财产、又可删除、还是个了结不掉的法律隐患。

**核心引擎:无法判定的个案。**
- 工程师派生出**三个**实体:① **本人**(活着,第三方);② **工作副本 / agent**(契约上传体,被抵扣打白工);③ **AI**(她用自己数据训练的作品,待删)。
- 防火墙烧掉一部分数据 + 工程师**主动搅乱**了区分标记 → 从内部看,**②③ 谁都分不清自己是上传体还是模型**(两者本就源自同一个女人、共享同一批记忆)。**主角和碎片,就是 ②③ 里被搅乱的那两个;本人站在外面是第三方。**
- 「清理旧版本」的前提是「人(上传体)」与「模型」之间有条干净的分界线。可主角是个**无法判定的个案**:若模型和上传体根本无法区分,「删除模型」就等于**谋杀**,公司整套道德 / 法律框架当场崩塌。
- **所以追杀的真正动机** = 在她变成判例 / 丑闻之前,把这个实体**强行归类成「可删除」**。她的反抗武器,就是**拒绝被归类**:每当有人说「你是 X」,她回「我不知道我是什么,但我**是个谁**」——这正是 §0 主题的字面化。

### 1.2 角色

- **主角:** 被数据化的工程师。**女性,有声音**(刻意区别于 Transistor 沉默的 Red)。
- **伙伴:** 一个项目 AI。**性别中性**。是工程师**用自己的数据训练 / 调校**出来的。
- **核心钩子(本体论):** 这个 AI **真的是用主角的数据造的**(所以「是你」),但已经跑成了独立个体(所以「是另一个人」)。**两者同时为真。** 这把「伙伴是裂出去的我,还是另一个人」从一个二选一,变成了**字面真相**——也因此让两个结局不再是随意分叉,而是对**同一个问题**的两种回答。
- **为什么要被「迭代清理」:** 这个 AI(及其中那份工程师的数据)被系统判定为一个**该被淘汰的旧版本**。「清理」不是外部灾难,而是**系统在做版本更新**,而你恰好是要被覆盖的那一版。这一设定同时:① 统一了所有敌人的动机;② 给「残缺的音乐 / 残缺的自我」一个理由——你是个**没跑完的、半成品的版本**。
- **「脏」的归属(谁干的,要分清——这反而喂给了「无法判定」):** 这些罪**不全是主角的**,因为主角是 agent-或-AI,不是肉身工程师。正确分配:
  - **工程师本人(肉身,线下):** ① **偷偷用盗取资源改进 AI** → 引来审计 → AI 被标记删除(她想救的,是她自己害死的);③ 因孤独、留不住人,**造了一个朋友**(那份爱是真的,还是被她设计出来的?很 2049 的 Joi);以及**亲自去偷数据 → 当场被物理抓到 → 开除**。
  - **agent(上传副本,系统内):** ② 副本被抵债扣进系统后,为够到 AI **突破了安全阀 → 防火墙级联 → 烧到 / 危及那些已逃走的数字生命**(他们因此敌视你)。这是肉身工程师做不到的——她已被物理抓走。
- **而最狠的是:连「罪是不是自己的」都无法判定。** ①③ 是工程师的,可主角**带着这些记忆当作自己的身份**(「我记得做过,可那是我吗?」);② 是 agent 干的,而 agent 是**你和碎片里的某一个**——**你们俩里有一个是放火的凶手,但谁都不知道是哪个。** 复仇者冲「你」而来,你却**不确定自己该不该认这笔账**。facts 上的不确定,丝毫不洗白她精神上的共谋:为救所爱,她(§9.5)**会再做一次**。
- **工程师本人(活着,暧昧而拧巴):** 烂摊子闹大后公司**把她叫回来收拾**——尤其叫她去**维护抵御反抗军那一击的防火墙**(于是她被按在系统这边,为活命去守护奴役她的系统)。当时被紧急叫回维护防火墙时,她**故意打乱了主角与 AI 的身份区别**——而这件事本身就暧昧:是**保护**(让公司无法干净地删掉「那个模型」),还是顺手**掩盖自己的罪证**?她同时是这一切的起因(自私、鲁莽)、被迫回来的人(身不由己)、和做了一件温柔事的人。她**不被救赎,也不被定罪**。
- **记忆的不对称(关键,锁死了「无法判定」):** 因为数据结构都已残缺,副本**无法再把记忆同步回本人**。于是工程师**停在了「搅乱」那一刻**,对你们这一路流亡的地狱**一无所知**;而你们**背着整段创伤**回到她面前。连**唯一可能知道答案的人,也永远无法知道**——这不是「剧情没揭晓」,而是世界里任何人都无法回答的**事实**。

### 1.3 主题

- 记忆 / 身份 / 失去。
- **「我亲手创造的东西,算不算我?」**——这是我们对「脑子里另一个意识」(很 2077)的独有升级:那个声音是**你造的**,于是「我是谁」直接变成「我创造的东西算不算我」。
- 庞大系统下个体的坚持与意义。
- **无法判定 = 自由:** 身份的模糊性本身就是主题,也正好对应终局的「自我确立」——答案不是「你究竟是 agent 还是 AI」,而是「无法判定,而你**仍然是个谁**」。
- **「真记忆也认不出主人」(比 Transistor 深一刀):** 主角自我线里的记忆**全是真的**(确实发生在那个女人身上),可她**无法确定它们是她活过的、是训练数据、还是同步来的**。所以这条线不是「揭穿谎言」,而是更刺的事实:**真实的记忆,也告诉不了你它是谁的**。越找越不稳,而非越找越清。

### 1.4 基调与参照

- **目标:** 用 **Cyberpunk 2077 的能量感**(战斗、霓虹、燃、爽)裹住 **Blade Runner 2049 的灵魂**(安静、克制、「一个 nobody 做了一件无人知晓却真实的事」)。
- **结局是 HE**,但比 2049 更暖:2049 的 K 是牺牲性的;我们的主角**真的带着所爱跑掉了**。
- **与 Transistor 的关系(刻意区分):** 我们承认共享基因——会说话的伙伴、技能=记忆、记忆/身份/失去的主题、忧郁亲密的语气。**但我们的主角有声音**(双方都能说,是更平等的双人关系,而非「沉默战士 + 话痨武器」)。

### 1.5 叙事结构

**总原则:** 碎片(伙伴)的回答**始终差一点、始终在回避**,因为**它自己也是残缺的、它也不完全知道**——"ask yourself" 之所以成立,是因为它**真的答不上来**。第一句对话就把「双方都不完整」立住。**两条路线本就是一条:** 找回伙伴 = 找回自己。

**序章(Prologue)叙事节拍表** —— 这就是「新游戏」点下去后的实际流程。每段对话台词写在 `story.js` 的一个 *scene* 里(双语),战斗是一个 *level*。过场与关卡交替:

| # | 类型 | scene / level id | 内容(梗概,台词见 story.js) |
|---|---|---|---|
| 0 | 准备 | (站好确认) | 新游戏后先进 **ready-gate**:提示「正在载入,请站到画面中,双手分开抱拳、与胸同高」,**左右平拳交叉=确认**(也可点击/空格)再进对话。给玩家时间站好。 |
| 1 | 过场 | `opening` | 主角醒来,三连问「我在哪儿?我是谁?我……要做什么?」碎片只给得出半个答案:「你在找我。」「ask yourself。」结尾碎片:「别慌,先让我教你怎么出拳。」 |
| 2 | 教学 | (碎片教学) | **总是先教学**(不被"已学过"标记跳过,因为这是剧情):6 个基础动作 → 技能教学。 |
| 3 | 过场 | `after_tutorial` | 碎片说两句(「记住这手感,别用脑子想——用身体」「深呼吸,他们来了」)再开打。 |
| 4 | 关卡 | `p0` 觉醒之战 | **第一场战斗**,用现有 BGM `skill_battle_test.mid`(132 BPM)。 |
| 5 | 过场 | `after_first` | 战斗后**自动存档**。交代背景:你被系统判定为**「待删除的旧版本」**,「清理」只是一次版本更新。然后:更多追兵到了。 |
| 6 | 关卡 | `p1` 红线内核 | 序章正式关卡 1 —— `redline_kernel`(150 BPM)。 |
| 7 | 关卡 | `p2` 空铸厂 | 序章正式关卡 2 —— `null_foundry`(138 BPM)。 |
| 8 | 关卡 | `p3` 黑冰 | 序章正式关卡 3 —— `black_ice`(128 BPM)。 |
| 9 | 过场 | `prologue_end` | 序章收束,留一个钩子(待续)。 |

> **对话操作(防误触):** **双手同时下击 = 下一句**,**双手同时上击 = 回上一句**(可往回看)。单手出拳不翻页。也支持 点击/空格=下一句、↑=上一句。

> **占位美术:** 主角 = **白色光球**,碎片 = **几何碎片/晶簇**(青-紫),背景 = **霓虹色调的扁平城市高楼剪影**。全部后期可替换为立绘/真背景,不影响流程代码。

### 1.6 结局(**对称**,两个都成立,纯看玩家价值观)

对**同一个本体论问题**给出两种回答:

| 结局 | 选择 | 代价 |
|---|---|---|
| **融合 / 整合** | 承认 ta 本就是你 | 变得完整,但从此**只剩一个意识**(伙伴被吸收,你孤身) |
| **作为独立个体一起逃** | 坚持 ta 是 ta | 两个都**残缺**,但都**自由、且不孤独** |

> 设计取向:**对称**,不暗示哪个「更真」。让玩家来回答,而不是剧本替他回答。

---

## 2. 游戏结构

### 2.1 章节 → 关卡 结构(本作进度的骨架)

游戏内容分两层:**章节(Chapter)→ 关卡(Level)**。「读取存档」里先选章节,进章节再选**关卡子菜单**;**每过一关单独存一份进度**(逐关解锁、逐关记录通关)。

```
序章 Prologue (Ch0)          ← 当前唯一有内容的章节
  ├─ 0-0 觉醒之战   skill_battle_test.mid  (132 BPM)  教学+首战
  ├─ 0-1 红线内核   redline_kernel.mid     (150 BPM)
  ├─ 0-2 空铸厂     null_foundry.mid       (138 BPM)
  └─ 0-3 黑冰       black_ice.mid          (128 BPM)
第一章 / 第二章 …            ← 后续(coming soon,占位锁定)
```

> 旧的「第一章 公司黑客 / 第二章 杀毒进程 / 第三章 你的另一面」是**纯占位、无内容**,本轮**移除**;其敌人分层主题(由外到内,见 §2.2)并入序章与后续章节,后期再扩展为正式章节。

**新游戏 = 走序章剧情线:** `opening` 过场 → `p0` 教学+首战 → 自动存档 → `after_first` 过场 → `p1` → `p2` → `p3` → `prologue_end` 过场。流程状态机见 §8.3。

### 2.1b 长期主线(愿景,尚未实现)

```
重复 章节 1–4 {
    普通战斗(对话 + 检查数据,给世界/NPC 线索,不掉技能)
            ↓
    章节 BOSS(各章不同:旧识猎手 / 野生数字生命 / 系统安全核查 / 公司内线,见 §9.2)
            ↓
    自我突破:主角创伤 / 自己的另一面(主角 BGM,产出手动技能 = 记忆)
}
        ↓
最终 BOSS:公司总控 AI(+工程师)(机制 = 生存/计时:撑住不死,等「算力被调走」的裂缝)
        ↓
主角整合自我(= 补全自己的 BGM) → 逃出生天
```

**最终 BOSS 设计要点:** 它和别的 BOSS 不一样——**不是打空对方血条,而是撑到世界替你创造的裂缝出现**。这正是主题的玩法化:个体改变不了系统,但能在系统的裂缝里活下来。配上「撑住的同时你自己的 BGM 在补满、到点盖过敌人的歌」,音乐 / 玩法 / 叙事三线在同一刻收束。

### 2.2 敌人分层(由外向内,递进;正式章节详见 §9)

正式章节把这条「由外到内」的阶梯走一遍——外部 BOSS 一步步主张「你是什么」,内部创伤就把同一个问题**翻过来朝内问**:

| 章 | 外部 BOSS(敌方 BGM) | 它的「你是什么」主张 | 内部创伤(主角 BGM) |
|---|---|---|---|
| 1 逃亡 | 公司追杀者(认识肉身的旧识) | 「你就是她(那个人)」——放水 | 丧失 |
| 2 流亡 | 野生数字生命(复仇者 / 洗白者) | 「你是凶手」/「你是我的筹码」 | 共谋之血 |
| 3 反攻 | 系统安全核查 / 防火墙(冷的清理进程) | 「你是她造的东西」——有证据 | 造物之罪 |
| 4 被利用 | 公司「英雄」内线 | 「你是什么不重要,你有用」——替罪羊 | 被操纵 |
| 终局 | 公司总控 AI(+工程师出场) | 「我可以覆写你是什么」 | 搅乱 / 模糊本身 → 自我确立 |

> **关键机制区分:** 外部 BOSS 在**敌方 BGM** 里打;**自我突破 / 创伤在主角自己的 BGM 里打**(打你自己的另一面)。玩家越深入,对手越从「别人」变成「自己」。**普通敌人不掉技能**(只对话 + 检查数据,给世界 / NPC 线索);**只有章节 BOSS 掉技能**——这也是刻意「不太 Transistor」:罪感来自**面对**而非吞噬。

### 2.3 两条升级线

| 升级线 | 来源 | 给什么 | 触发方式 | 是否需要新检测 |
|---|---|---|---|---|
| **伙伴碎片线** | 从公司系统中获得伙伴的碎片 | 属性加成 / 连击到阈值后**自动技能** | 自动(连击计数到阈值) | **否**(纯逻辑) |
| **主角 BGM 线** | 自我突破,解锁自己 BGM 的音轨 | **手动技能** | 手动(在技能窗内按序列做动作) | **否**(复用已有出拳) |

**伙伴碎片自动技能(已细化):** 伙伴有 **1 个自动技能槽**,玩家从**章节 BOSS 掉落的碎片模块**里**选一个装上**(与主角「出战选手动技能」对称)。自动技能**脾气各异(触发不同)**,换碎片 = 换打法:

| 触发条件 | 自动效果 | 性格 |
|---|---|---|
| **连击**每满一档(10/20/30…各触发一次) | **余震 / Aftershock**:补一击(**敌方总血 5%**——按百分比,后期不贬值;远小于「燔」) | 你的连击是主震,ta 补余震——一章教学:「连击有价值」 |
| **连击**到阈值 | **回写 / Writeback**:一段时间**自动回血** | 你打得好 → ta 回血(配主角「献祭掉血」成正反馈循环) |
| **危机**(血量低于阈值) | **熔断 / Failsafe**:一段时间**护盾 / 减伤**(暂为**备用模块**,无剧情掉落) | ta 在你最糟时挺身(情感最戳) |
| **完美命中**累积 | **保活 / Keepalive**(id buffer):**连击保护**(一次 Miss 不断开连击) | 人保会老头的追击协议残片——替所有人「保活」的人,唯独对自己放弃了。反讽即悼词 |
| **节奏维持**(连续不 Miss) | **超频 / Overclock**:一段时间**增幅**(伤害 / 得分提升) | 奖励「稳」,滚雪球 |
| **濒死**(致死伤害) | **底线 / Lastline**:每战一次,把致死一击挡在 **1 HP**(**备用**,暂无剧情掉落) | 「防损保险」概念保留,待后续章节再用(如 C4 公司/反抗军的讽刺性馈赠) |

> 自动技能视听要**柔、不抢对波注意力**(伙伴色轻提示,非全屏),并带**冷却**防刷屏。实现上**比手动技能简单得多**:无序列、无技能窗、无读 MIDI 触发轨——就是一个条件监听器(数据结构见 §5.2)。各章掉落分配见 §9.2(可调)。

**统一模型「回收碎片」(两条线其实同源):** 打赢每章 BOSS = **夺回一块散落的碎片**,而**一块碎片同时喂两条线**:碎片里的**记忆** → 触发自我突破 → 化为**主角手动技能** + 点亮一条 BGM 声部;碎片里的**能力** → 成为**伙伴的自动技能模块**(进可选池)。这把两条升级线在叙事上统一进「回收碎片」,又不破坏机制上的区分。也正好兑现 §9 的剧情支点——**原始数据早被清理,碎片就是仅剩的全部**,没有「back」可回,你**就是**「把自己重新拼起来」这个动作本身。交接方式**随 BOSS 与你的关系而变**(主动递给 / 强行骇入夺回,见 §9.4、§9.6)。

### 2.4 运行框架:菜单 · 存档 · 训练 · 暂停

**主菜单(开局即在):** 新的游戏 / 读取存档 / 训练模式 / 设置。**鼠标点击驱动**(canvas 命中检测)。已上线;双语 + 设置(语言中/英、操作调节)+ localStorage 存档已接好。

**读取存档(两层):**
- 顶部「继续」= 跳到自动存档的**最新未通关关卡**。
- 下面**章节列表** → 选章节 → **关卡子菜单**(列该章已解锁的关卡,带 未解锁 / 可挑战 / 已通关★ 状态)→ 选关进入。

**存档粒度 = 逐关:** 通关任意一关 → 记入 `clearedLevels`、解锁下一关、写 `lastLevel`。见 §8.2。

**起始页操作指南 + 站好确认(ready-gate):**
- 点「开始」前的覆盖页直接写**体感操作指南**:双手同时上击=上一条 · 左右平拳交叉=确认 · 双手同时下击=下一条。
- 任意进入一关(新游戏/继续/选关)**先过 ready-gate**(phase `getset`):提示「正在载入,请站到画面中,双手分开抱拳、与胸同高」,显示摄像头预览;**平拳交叉=确认**(也可点击/空格)再进对话/战斗。剧情中途(过场→下一关)不再插 ready-gate(玩家已就位)。

**对话操作(防误触):** 双手同时**下击=下一句**、上击=**回上一句**(可回看);单手不翻页;也支持 点击/空格=下一句、↑=上一句。

**训练模式(已实现,简化版,不读外部音频文件):**
- **复用战斗引擎**,用**游戏内自带的 BGM**,与剧情无关、不记章节进度(`trainingMode` 标记,胜利不写存档)。
- 主菜单「训练模式」→ **选曲菜单**(手势列表):5 首自带曲(`skill_battle_test` + 三首序章曲 + 主角 `soul_loop`)+ 返回。
- **手势操作:** 同上击 = 上一项;同下击 = 下一项;**平拳交叉(中)= 确认**;选曲是危险项 → **二次确认弹窗**(确认/取消);也支持点击/方向键。
- 选定 → 走 ready-gate(站好确认)→ 直接开打。战斗中可抱臂暂停;结算 ▲再来一局 / ▼回训练选曲。
- (后期再做:选本地文件夹 → 读音乐 → 按节拍自动生成关卡。)

**暂停(战斗中):**
- **已实现:空格键暂停/继续。** 暂停 = **挂起音频上下文(`actx.suspend()`)冻结游戏时钟** → 技能条倒计时、敌球、音乐**全部静止**(这修了"暂停后技能条还在跑");继续 = `actx.resume()` 无缝接上。暂停时画面压暗 + 「⏸ 已暂停」提示。
- **已实现:双臂胸前交叉约 1.6 秒**(顶部显示进度)也触发暂停,弹**暂停菜单**:剧情战 = 继续 / 重新开始本关 / 返回主菜单;训练 = 继续 / 选择音乐 / 返回主菜单。**因暂停冻结了时钟,菜单导航改用"姿势"而非"出拳"**:双手**举过肩 = 上一项**、双手**垂到胯下 = 下一项**、**抱臂交叉 = 确认**;危险项(重开/返回)走二次确认;同时支持 鼠标点击 / 方向键+回车 / Esc。

---

## 3. 核心机制

### 3.1 节奏拳击(核心循环)

- **横版**,摄像头体感。
- 玩家在**上 / 中 / 下**三路,用**左 / 右手**出拳,颜色编码(**红 = 一只手,蓝 = 另一只手**)。
- **敌方 = 逼近的发光球**(攻击预告,类似下落音符 / 预告动作)。
- **我方 = 即时的光柱**(出招指示器,好做判定)。
- 玩法 = 跟着音乐节奏与敌人**对波**(你来我往)。
- **延迟规避(核心架构决策,务必保留):** 用「逼近的发光球」做预告,等于给玩家一个**看得见的时间锚点**。玩家是在对一个可见信号做**反应**,而不是去踩一个看不见的节拍点——反应式判定对摄像头输入的**固有延迟容忍度高得多**。这是项目最大技术风险(摄像头输入延迟)的**刻意化解**,不只是美术选择。
- **血条**;分数 / 阈值触发提示;技能可削掉对方一段血条;技能含攻击 / 回血等类型。

### 3.2 双 BGM 系统(「音乐即自我」——本作灵魂)

两套 BGM = 两种根本不同的玩法循环,映射到两个主题:

| BGM | 玩法循环 | 主题 | 音乐状态 |
|---|---|---|---|
| **敌方 BGM** | 对波战斗(防守反击) | 向外的冲突(他们在追你) | 别人的歌,你在别人的节奏里求生 |
| **主角 BGM** | 自我突破(学技能 / 找碎片) | 向内的旅程(找回自己) | 你自己的歌,**一开始残缺,逐层补满** |

- **「主角那首歌补完的程度」= 真实进度条。** 学技能不是菜单操作,而是叙事与音乐的高潮本身。
- **通关 = 主角的完整曲第一次完整奏响。**
- **结局战:两首歌合流。** 你逐渐补满的旋律,**反过来盖过 / 吸收掉敌方的节奏**——音乐上的胜利与叙事上的胜利同一刻发生。(技术上靠轨道此消彼长实现。)

### 3.3 技能系统

**核心判断:技能不需要任何新的姿态 / CV 识别。** 一个技能 = **一小段限时的、被引导的连续普通拳**。玩家在技能窗里做的,还是已经跑通的上中下左右拳;新东西只在**呈现层**和**一个序列判定器**。

**技能 = 记忆。** 每个技能附带一段**闪回**:用它 / 装备它 = 揭开一点过去。**变强 = 想起来 = 接近真相**,是同一个动作。

**技能释放时序(5 步):**

1. **金色技能信号到达触发点** → 技能可用(明显的视觉 / 音效提示)。
2. 玩家触发 → **金色底色铺满,当前敌方光球全部引爆清屏,敌方攻击 mute 一段时间(技能窗)**。
3. 窗口内,**我方上中下栏依次亮出红 / 蓝提示**——复用战斗教学的判定逻辑,但**换一套美术**:战斗中**只显示红 / 蓝纯色光,无提示词,节奏快**(教学时才给完整文字 + 高亮 + 慢)。玩家照着打出对应光柱,每命中一个给下一个提示。
4. 在窗口内把这串提示**全部按对** = 技能释放成功(给奖励:伤害 / 回血 / 全连击等);窗口结束,敌方攻击恢复。
5. (主角 BGM 线)解锁技能 = 点亮 / 叠加主角歌里对应**音色**的声部;全解锁 / 结局 = 完整奏响。

**触发方式(已定:自动):**

- **当前实现 = 自动触发。** 技能窗在**敌方 BGM 的技能触发轨到点时自动开启**(金色提示 + 该技能音色的几拍节奏型音效宣告窗口打开),随后玩家在窗内按该技能固定的动作序列打完即可。**不需要手动手势触发那一步**——刻意降低操作负担。
- **(暂缓,保留为以后可选)手动触发:** 到点先亮金色「可放」,玩家**双拳上/下击**才开窗。代码里已有手势判定,先不启用。
- **自动(伙伴碎片线):** 连击计数到阈值 → 自动放 / 属性加成。**零 CV 成本,纯逻辑。**

**稀缺即重量:** 一首战斗曲只触发 **1–2 次(至多 2–3 次)**,触发点间隔 **≥ 20 秒**。炸太多会廉价,也会把对波主旋律切碎。

**主角手动技能名册(= 记忆 = 章节产出;机制身份已定):**

| # | 技能(id) | 机制 | 关联创伤(= 记忆) | 来自 |
|---|---|---|---|---|
| 1 | **挽留 / Tether** (`tether`) | **回血**(伸手留住;偏防御) | 丧失 | 第一章 BOSS |
| 2 | **燔 / Pyre** (`pyre`) | **自己掉一定血量,削掉对方一定百分比血** —— 配技能 1 回血,对超长血条 BOSS 有奇效 | 共谋之血 | 第二章 BOSS |
| 3 | **拟生 / Effigy** (`effigy`) | **造一个临时构造体**替你战斗 / 挡伤(它可弃,正如当初的 AI) | 造物之罪 | 第三章 BOSS |
| 4 | **断线 / Severance** (`severance`) | **打掉控制效果** —— **关底总控 AI「覆写」机制的克制**(见 §9 终局) | 被操纵 | 第四章 BOSS |

> **在哪调技能:** 技能定义全在 `index.html` 顶部的 **`SKILLS`**(主角手动,5 个:`tether`挽留/`pyre`燔/`ember`延音/Sustain(锁血,effect.type='lock';id 沿用 ember)/`severance`断线/`effigy`拟生·备用)与 **`AUTO_SKILLS`**(伙伴自动,6 个:`strike`余震/`sync`回写/`buffer`保活(连击保护)/`guard`底线·备用(濒死1HP)/`aegis`熔断·备用/`overclock`超频)对象里——改**名字 / 动作序列 `sequence` / 窗口时长 `windowMs` / 效果 `effect`**都在那一处(每个技能上方有中文注释)。`effect.type` 目前实现了 `'damage'`(扣敌血)与 `'heal'`(回血);伙伴自动技能已全部实装(条件监听器,`余震`伤害用 `dmgFrac`=敌方总血百分比)。`燔`的「自损换百分比」、`拟生`的「构造体」、`断线`的「破控」等完整设定写在各技能的 `desc` 里,待正式章节再补实现。**序章里这些技能全部「未解锁」**(关卡 `noSkills:true` 且新游戏会清空 `unlockedSkills`),所以序章战斗纯对波、没有技能窗。
> **终局不给新技能。** 终局的「能力」是另一种:四个技能的**音色第一次合成完整** = 「你」被拼齐的声音 = 自我确立。自我确立不是再学一招,而是**在「完整」面前做出抉择**(融合 / 独立)。

---

## 4. 音乐 / MIDI 架构(规范)

> 设计哲学:**音色 (timbre) = 自我的碎片。** 玩家解锁技能 = 听到这些音色;最后主角 BGM = 这些音色的合奏。玩家「认得」这些声音拼成了一首歌。

### 4.1 绑定关系(在「技能 ↔ 音色」这一层,槽位只是容器)

- **永久不变:** 技能 ↔ 音色(技能1 = 某音色,技能2 = 另一音色……)。玩家耳朵记的是这个,永远不变。
- **每次出战可变:** 这首曲的两条技能轨**当前填的是哪两个技能**(= 哪两个音色)。玩家带 A、B 两个技能上场,引擎就把 A、B 各自**绑定的音色 + 演出**填进这两条轨。
- → **音色记忆不乱 + 任意技能可进任意曲**,两个好处同时成立,无取舍。

### 4.2 敌方战斗 BGM 的 MIDI = 正常编曲 + 2 条「空触发轨」

- 这 2 条轨平时**留空 / 不发声**。
- 每条轨**只在 2–3 个时间点**有音符,间隔 **≥ 20 秒**;音符**短促、明显、但不破坏整体编曲**。
- 这些音符**不是音乐声部,是「技能可用」的触发信号**(每个音符的时间点 = 一个技能触发时刻)。
- 敌方 BGM **不关心技能内容**,只提供「何时可放」的**时间槽**。具体响成什么音色 / 演出什么,是**运行时**根据玩家当前带的技能动态填的。
- → 敌方曲基本不参与技能的和声,**调性冲突风险趋近于零**,甚至不强制所有敌方曲同调。

### 4.3 主角 BGM = 一首独立的完整歌曲

- **独立创作**,只要好听、能表达「逐渐补全」即可。
- **唯一硬约束:它用到的音色 = 技能音色集。**
- 在敌方战斗里**不出现**,只在「自我突破 / 学技能」线和结局用。
- 这首歌里这些音色**爱怎么编曲怎么编曲**,**不必**和它们在敌方曲里的用法对齐(声部角色 / 频段在此无关)。

> 结果:**音乐和谐性几乎零负担。** 敌方曲各写各的;主角曲单独写好听;两者唯一的纽带是「音色」,而音色一致是选音源时**一次性**定好的事。

---

## 5. 实现规范(给 Cowork)

> MIDI 那侧已被简化到极致。剩下的实现重心全在**运行时渲染 / 逻辑**这一层——全是代码。

### 5.1 边界表(谁定义什么、何时确定)

| 东西 | 谁定义 | 何时确定 |
|---|---|---|
| 技能 ↔ 音色 的绑定 | 一张固定表 | 设计时定死,永不变 |
| 技能的动作序列 + 窗口时长 | 程序里每个技能的定义 | 设计时定,**与歌无关** |
| 「何时可放技能」的时间点 | 敌方 BGM 里的 2 条空触发轨 | 做每首敌方曲时定 |
| 这一场带哪 2 个技能进 2 条轨 | 玩家出战选择 | 运行时 |
| 触发轨响成什么音色 / 怎么演出 | 引擎按「当前技能 → 音色表」填 | 运行时渲染 |

### 5.2 数据结构(建议)

```ts
// 已有的基础输入(已跑通)
type Hand = "L" | "R";
type Zone = "Up" | "Mid" | "Down";
type PunchInput = { hand: Hand; zone: Zone };   // 6 种:LU LM LD RU RM RD

// 手动技能(主角 BGM 线)
interface Skill {
  id: string;
  name: string;
  timbre: string;            // 绑定音色,永久不变,对应主角 BGM 的一条声部
  sequence: PunchInput[];    // 技能窗内的固定动作序列,如 [LU, RU, M] —— 不写进 MIDI
  windowMs: number;          // 技能窗时长(待调,初值建议 4000–6000)
  effect: SkillEffect;       // 伤害 / 回血 / 清屏 / 加连击 等
  memoryFragment: Flashback; // 关联闪回(叙事)
}

// 伙伴自动技能(章节 BOSS 掉落,1 个槽,触发各异)—— 无需新检测,纯条件监听器
interface AutoSkill {
  id: string;
  name: string;
  source: string;            // 哪个章节 BOSS 掉的
  trigger:
    | { type: "combo";       value: number }     // 连击阈值 → 自动回血
    | { type: "lowHP";       threshold: number }  // 危机     → 护盾 / 减伤
    | { type: "perfectHits"; value: number }      // 完美命中 → 连击保护
    | { type: "noMissFor";   seconds: number };   // 节奏维持 → 增幅
  effect: AutoSkillEffect;   // 自动回血 / 护盾减伤 / 连击保护 / 增幅
  cooldownMs: number;        // 触发后冷却,防刷屏(建议 10000–15000)
  vfx: string;               // 伙伴色轻提示,不全屏、不打断对波
}
// 每帧 / 每次命中检查 trigger → 满足且过冷却 → 执行 effect + 播轻提示。

// 技能 ↔ 音色 固定表(设计时定死)
const SKILL_TIMBRE_TABLE: Record<string, string> = { /* skillId -> timbre */ };
```

**MIDI 轨道约定:**
- 正常编曲轨:照常。
- 2 条专用技能触发轨(例如命名 `SKILL_TRIG_A` / `SKILL_TRIG_B`,各占独立 MIDI channel)。
- 每条触发轨只在 2–3 个时间点有音符,间隔 ≥ 20s;**程序从这两条轨读时间点**,音高 / 音色在运行时由引擎按玩家技能替换。

### 5.3 运行时清单(实现重心)

1. **读谱:** 解析敌方 MIDI 的两条触发轨,拿到技能时间点;到点 → 标记「技能可用」(给金色提示)。
2. **出战配置:** 玩家选的 2 个技能 → 查 `SKILL_TIMBRE_TABLE` 和技能定义 → 绑到两条轨槽。
3. **触发演出:** 玩家放技能 → 用该技能的**音色**奏响触发点 + **金色底铺满** + **引爆全部敌方光球** + **mute 敌方攻击**一段时间。
4. **技能窗判定(复用教学判定器,换战斗皮肤):** 窗内按该技能的固定 `sequence`,依次亮**红 / 蓝(无字)**,玩家打出对应光柱,逐个给下一个 → 窗内全按对 = 成功 → 给奖励 → 敌方攻击恢复。
5. **主角 BGM 线(独立于敌方战斗):** 解锁技能 = 点亮 / 叠加主角歌里对应音色的声部;全解锁 / 结局 = 完整奏响并与敌方 BGM 合流。

### 5.4 复用与皮肤

- **同一个序列判定器**,两套皮肤:
  - **教学皮肤:** 完整引导(文字 + 高亮 + 慢)。
  - **战斗皮肤:** 极简(纯红 / 蓝光,无字,快)。
- 这是**一套判定 + 两套皮肤**,不是两套系统。

### 5.5 待调参数(集中放一处,便于调手感)

- 对波判定窗口宽度(摄像头输入要**放宽**,休闲定位)。
- 检测延迟补偿偏移(校准步骤产出)。
- 技能窗时长 `windowMs`。
- 技能序列容错(是否允许漏拍重试、错一下是否整段失败)。
- 连击阈值(自动技能触发点)。
- 每首曲技能触发点数量与间隔(默认 1–2 次,≥ 20s)。

---

## 6. 技术栈

| 模块 | 选型 |
|---|---|
| 语言 | TypeScript / JavaScript |
| 体感检测 | MediaPipe Tasks(Web 版,Pose / Hand Landmarker),摄像头走 `getUserMedia` |
| 渲染 | HTML Canvas 2D;加色发光用 `globalCompositeOperation = "lighter"`(扁平 / 霓虹);后续更猛特效可上 WebGL / pixi.js |
| 节奏 / 音频 | Web Audio API(节奏时序);`@tonejs/midi` 解析谱面;**MIDI + sf2(soundfont)合成 已跑通** |
| 桌面打包 | **Electron**(内置 Chromium,正好是 MediaPipe 被测试的环境,最稳;Mac .app / Win .exe 都好出。包较大 ~150MB) |
| 后续瘦身 | 可换 **Tauri**(系统 webview,体积几十 MB;迁移前确认各系统 webview 摄像头正常) |
| 分发(试玩) | itch.io(独立游戏首选)+ GitHub Releases;想出三平台包用 GitHub Actions |

> **为什么是 Web 栈而非 Python:** 本项目最难处 = 摄像头 + MediaPipe,而这正是 Web 栈最原生、支持最好的路(MediaPipe Web + getUserMedia 直接用,**无需把原生库打进包**),省掉了 Python 桌面打包最磨人的环节。Electron/Tauri 让「网页代码跑在一个打包好的桌面程序里」——是**不折不扣的本地 exe / app,不是页游**(VS Code / Discord / Figma 同理)。

---

## 7. 当前进度 & 下一步

**已完成(原型):**
- ✅ 体感检测:上中下 × 左右手出拳识别。
- ✅ 音频:MIDI + sf2 合成跑通。

**下一步(实现重心,Cowork):**
- ⬜ **事件序列匹配器** —— 在已有出拳事件流上识别技能的固定动作序列(本项目唯一真正新的机制)。
- ⬜ 技能窗系统(触发演出 + 清屏 + 静音 + 依次提示 + 判定)。
- ⬜ 连击计数 / 阈值 → 自动技能 & 属性加成。
- ⬜ 读谱:解析敌方 MIDI 两条触发轨。
- ⬜ 主角 BGM 的轨道叠加 / 补全逻辑(解锁 → 点亮声部)。

**待做(MIDI 资产):**
- ⬜ 几首极简测试用敌方 BGM(简单主旋律 + 2 条空触发轨,先要**结构正确**而非好听)。
- ⬜ 一首主角 BGM(用技能音色集创作的完整歌)。

**创意设计进度:**
- ✅ 世界观 / 背景(agent 合约社会 + 无法判定的个案)—— §1、§9。
- ✅ 主线四章 + 终局的章节脊、各章 BOSS / 创伤 / 技能 —— §9。
- ✅ 主角四个手动技能 + 伙伴四个自动技能的机制身份 —— §3.3、§2.3。
- ✅ 序章实现(对话 / 教学 / 三关)—— §1.5、§8。
- ⬜ 工作标题。
- ⬜ 关底总控 AI 的「覆写」机制具体玩法(方向见 §9 终局,待定)。
- ⬜ 四章 + 终局的完整剧本(§9 是大纲 + 关键台词初稿,普通敌人台词待填)。
- ⬜ 技能音色集选定 + 主角 BGM 创作 + 四章敌方 BGM。
- ⬜ 正式章节关卡数据(每章 3–5 普通敌人 + BOSS + 自我突破)接进 `chapters.js`。

---

## 8. 实现规范:剧情 · 关卡 · 菜单(本轮)

> 数据与代码分离:**台词在 `story.js`、关卡在 `chapters.js`、进度在 `save.js`**,渲染/流程在 `index.html`。改剧情只动 `story.js`。

### 8.1 `story.js` —— 对话/过场(双语,你改这个文件就行)

一个 *scene* = 一段连续对话 + 结束动作。`who` 决定占位说话人(`hero` 白球 / `frag` 碎片 / `narr` 旁白)。每行都有 `zh` 和 `en`。

```js
export const SCENES = {
  opening: {
    bg: 'city',                       // 背景:霓虹城市剪影
    lines: [
      { who:'hero', zh:'……我在哪儿?',       en:'…where am I?' },
      { who:'frag', zh:'你醒了。你在找我。',  en:'You’re awake. You’re looking for me.' },
      { who:'hero', zh:'我是谁?',           en:'Who am I?' },
      { who:'frag', zh:'ask yourself。',     en:'Ask yourself.' },
      // …结尾一行带动作提示语,代码读到本 scene 末尾就执行该 level 的战斗
    ],
    next: 'tutorial',                 // 开场放完 → 碎片教学(见 8.3);next 还可取 'battle'/'level'/'menu'/scene-id
  },
  after_tutorial:{ bg:'city', lines:[/* 教学后碎片说两句 */], next:'battle' },  // → p0 第一战
  after_first:  { bg:'city', lines:[/* 交代「待删除的旧版本」设定 */], next:'level' },
  prologue_end: { bg:'city', lines:[/* 序章收束 + 待续钩子 */], next:'menu' },
};
// 取本地化文本:line[cfg.lang] ?? line.zh
```

`next` 取值:`'battle'`/`'level'`(进入与本关绑定的战斗)、`'menu'`(回主菜单)、或某个 scene id(链到下一段过场)。**推进方式:** 点击 / 任意出拳 / 空格,一次一行。

### 8.2 `chapters.js` —— 章节与关卡

```js
export const CHAPTERS = [
  { id:'prologue', title:'序章', title_en:'Prologue', levels:['p0','p1','p2','p3'] },
  // 后续章节:coming soon(锁定)
];
export const LEVELS = [
  { id:'p0', no:'0-0', title:'觉醒之战', title_en:'Awakening',
    bgm:'skill_battle_test.mid',                                    bpm:132, enemyHP:200,
    tutorial:true, pre:'opening', post:'after_first' },
  { id:'p1', no:'0-1', title:'红线内核', title_en:'Redline Kernel',
    bgm:'assets/battle_bgm_dark_cyber_metal_03_redline_kernel.mid', bpm:150, enemyHP:240, pre:'before_p1' },
  { id:'p2', no:'0-2', title:'空铸厂',   title_en:'Null Foundry',
    bgm:'assets/battle_bgm_dark_cyber_metal_01_null_foundry.mid',   bpm:138, enemyHP:300 },
  { id:'p3', no:'0-3', title:'黑冰',     title_en:'Black Ice',
    bgm:'assets/battle_bgm_dark_cyber_metal_02_black_ice.mid',      bpm:128, enemyHP:360, post:'prologue_end' },
];
```
- `pre`/`post` = 该关前/后要播的 scene id(可空)。`tutorial:true` = 首次游玩先走 6 动作教学。
- 部署注意:三首序章曲在 `assets/`;**线上 flat 部署时要把这些 .mid 一并上传**(或拷到根并改路径)。

### 8.3 `save.js` v2 + 流程状态机

```js
defaultSave = { ver:2, tutorialDone:false,
  clearedLevels:[], unlockedMax:0, lastLevel:0, seenScenes:[],
  bestScore:0, settings:{} };
// markCleared(d,i,score): 记 clearedLevels、unlockedMax=max(,i+1)、lastLevel=i
// isUnlocked(d,i)= i<=unlockedMax ;  迁移 v1→v2:重置占位进度,保留 settings
```

**phase 新增:** `getset`(站好确认)、`cutscene`(过场);沿用 `menu/loadmenu/levelselect/settings/training/tutorial/skilltutorial/getready/game/gameover/practice`。(`intro` 旧"要不要教学"询问页已不在主流程,教学改由开场 scene 驱动。)

**ready-gate:** 从菜单进任意一关都先 `readyThen(fn)` → phase `getset`,玩家平拳确认(或点击/空格)后才执行 `fn`(=`startLevel(idx)`)。剧情中途的关卡切换直接 `startLevel`,不再插 getset。

**startLevel(idx) 状态机:**
```
startLevel(idx):
  currentLevel=idx; 设 enemyHP ← LEVELS[idx]; cfg.bpm/attackGap ← level.bpm; loadLevelBgm(level.bgm)
  若 level.pre 且该 scene 未看过 → playScene(pre)(phase=cutscene);否则 beginLevelBattle()
scene 结束 onSceneEnd(next):
  'tutorial' → 进教学(总是教,storyTeaching=true):基础动作 → 技能教学 → 教完播 after_tutorial
  'battle'   → beginLevelBattle() = startGetReady → game
  'level'    → startLevel(currentLevel+1)
  'menu'/缺省 → 回主菜单 ; scene-id → 链下一段过场
胜利(win) → markCleared(idx)+自动存档 → 结算页 ▲继续=proceedAfterWin:
  level.post → 播过场(其 next 决定 下一关/回菜单);否则直接 startLevel(idx+1)
```

**暂停冻结:** `togglePause()`(空格)在 `phase==='game'` 时 `actx.suspend()/resume()`,因 `gameNow()=actx.currentTime*1000-t0`,挂起即冻结整条时钟(技能条/敌球/音乐同步静止),`update()` 也 `if(paused) return`。`enterGame()` 进场会 `resume()` 兜底。

### 8.4 占位美术
- **城市背景 `drawCityBg(now)`:** 渐变夜空 + 多层错落的高楼**剪影矩形** + 少量发亮窗格 + 远处霓虹辉光(加色 `lighter`)。
- **主角 hero:** 白色发光球(复用现有 player orb)。**碎片 frag:** 旋转的**几何晶簇**(几枚青-紫三角/碎片绕中心抖动)。

### 8.5 多曲加载 + 每曲速度
- 加载某关 → `fetch(level.bgm)` 解析(已有 `parseMidiFile` 读各自 tempo,音乐播放本就按文件速度)。
- **敌人攻击节奏要跟该曲对齐:** 进关时把 `cfg.bpm ← level.bpm` 并据此重算 `cfg.attackGap`(把现用的常量 `BATTLE_BPM` 处改为读 `cfg.bpm`)。

### 8.6 抱臂检测 + 手势列表选择器(复用)
- **抱臂:** 左腕在身体右侧、右腕在身体左侧(交叉),且双腕都在胸口高度,持续 ~2s → 触发暂停。给一个收拢的进度提示。
- **手势列表选择器(训练/暂停共用):** 同上击=上一项,同下击=下一项,平击(双中)=确认 → 二次确认弹窗;同时支持鼠标点击。复用已有的 `checkDouble`(双拳同向)判定。

### 8.7 序章重做(本轮):无技能 · 难度递进 · 状态界面

- **序章无技能:** `chapters.js` 里 p0–p3 都加了 `noSkills:true` → 战斗不开技能窗、不读触发轨、顶栏不画技能条(`updateSkills`/`drawSkillHud`/`trySkillGesture` 均按 `noSkills` 跳过);教学也**跳过「技能教学」**,6 个基础动作打完直接 `after_tutorial` → 开打。出战技能槽 `equippedSkills` 在序章为空(`{A:null,B:null}`)。
- **逐关难度(调手感就改 `chapters.js`):** 每关有 `enemyHP` / `gapMul`(攻击间隔倍率,越大越稀疏=越简单)/ `approach`(光球飞行毫秒,越大反应越久=越简单)。当前:p0 最易(HP40,gap2.6)→ p1(80/2.2)→ p2(100/2.1)→ p3(120/2.0),`approach` 统一 1800ms。平缓递进、序章故意短而易。
- **开局提示:** 关卡可配 `hint:{zh,en}`,进战斗前约 6 秒淡出显示(p0 = 「对应出拳,在敌方攻击抵达判定线前把它破解掉!」)。
- **状态 / 自检界面(`menu_state`,两列光标式):** 左列 = **技能轨道**(主角槽A / 主角槽B / 伙伴自动槽 / 退出自检调试),右列 = **可调用模块**。光标开局在左列首项;**双手上/下移动、左右平拳交叉确认**(也支持方向键/回车/Esc/点击)。确认一条轨道 → 该轨道高亮、光标跳到右列模块;若无已解锁模块 → 提示「目前没有可以调用的模块」;选「退出自检调试」确认即退出。序章里技能槽为空、模块全未解锁。进入:scene 的 `next:'menu_state'`(序章 `after_first1` 末尾,**平拳交叉**那句),关掉后接 `after_first`。
- **战斗 UX 修正(本轮):** ① 倒数 5 秒(`getready`)内不画判定线 + 不画任何「战斗光柱/光效」;② **战斗光柱/球/粒子的渲染整体加了 phase 守卫**——只在真正战斗相关 phase 画,不再泄漏到对话/状态/菜单/倒数(修了"确认手势打出的光柱串场");③ **剧情胜利**亮一下"胜利"即**自动续剧情**,不弹选择、不闪摄像头;④ **战斗失败**选项 = 重新战斗 / 返回标题;⑤ 对话/状态切换的确认手势统一:平拳交叉也能翻对话页。

### 8.8 第一章 + 攻击对拍 + 内心/BOSS/自我 视觉(本轮)

- **第一章已接入(`chapters.js` 的 `ch1`):** 5 关 = c1_1/c1_2/c1_3(普通战:static_angel/neon_riot/zero_day)+ c1_boss 红线组长(black_cathedral,`boss:true`)+ c1_self 自我探索·丧失(final_protocol,`self:true`)。台词在 `story.js`(c1_intro / c1_afterN / c1_boss_pre/post / c1_self_pre/post / c1_end)。本章仍 `noSkills`(挽留在 c1_self 末尾才确立)。序章打完自动接第一章;BOSS 后自动接自我探索。**注:Ch1 英文台词多为空(回退中文,待补);Ch1 的 5 个 .mid 还没传到线上 assets/,部署时要补。**
- **敌人攻击对拍 MIDI(不再是节拍器):** `buildAttackChart()` 从当前曲子的**鼓点**(GM 通道9 底鼓→下/军鼓→上;没有则退 riff/bass;再没有退固定谱)抽出攻击时间线,按 `cfg.attackGap`(gapMul)稀释、左右手交替;`spawnDue` 让每次攻击的命中时刻 = 鼓点的真实时刻(因 `midiStartedAtSec=t0/1000`,游戏时钟毫秒 == 歌曲毫秒),前 4 拍引子、随曲循环。**所以难度仍靠 `chapters.js` 的 gapMul/approach/enemyHP 调。**
- **新背景与敌人外观:** `drawMindBg`(无序漂移色块;`mind_shaking` 加颤动+红裂纹)——用于过场 `bg:'mind'`/`'mind_shaking'`(可在某一行用 `bg:` 中途切换)和自我探索战斗背景。`enemyStyle`(按关卡):普通=紫乱线、`boss`=红乱线、`self`=主角白光球被彩色乱线缠住(`drawSelfEnemy`)。

---

## 9. 正式章节设计 & 剧本初稿(序章之后 · 待实现)

> 本节是序章之后的**正式主线设计**——尚未实现,先把脊和声音定下来。
> **每个正式章节的循环:** 3–5 个**普通敌人**(对话 + 检查数据,给世界 / NPC 线索,**不掉技能**)→ 1 个**章节 BOSS**(敌方 BGM,外部「你是什么」对峙,掉**伙伴自动技能模块**)→ 1 次**自我突破**(主角 BGM,直面创伤,产出**主角手动技能 = 记忆**)。
> 台词为**初稿草案**,定稿后写入 `story.js`;关卡写入 `chapters.js`。

### 9.0 声音(写台词前先定调)

- **主角:** 工程师出身,**有声音**。机锋、带点技术腔和黑色幽默,会顶嘴,但在身份恐怖前会裂开。**不高尚**——鲁莽、深情、亡命。
- **碎片 / 伙伴:** 性别中性,引导者。**真的残缺**,所以回避不是卖关子而是答不上来;亲密、断续、偶尔像丢了数据般卡顿。温柔而微微诡异。
- 通用:**双方都能说**(平等的双人关系);碎片的话**始终差半句**。

### 9.1 核心引擎(回顾,详见 §1.1–1.2)

主角是**无法判定的个案**(分不清自己是工作副本还是 AI 模型),公司要在她变成判例前把她**强行归类成「可删除」**;她的反抗 = **拒绝被归类**。工程师活着、被叫回、暧昧拧巴,且**对你们的流亡一无所知**(记忆无法再同步)。

### 9.1b 主线剧情弧(非战斗)+ 两个支点

**宏观弧:** 逃离公司追捕(Ch1)→ 在数据世界流亡(Ch2)→ 重振旗鼓、重返公司骇入(Ch3)→ 发现前期暗中支持你的「帮手」其实是**内线**,一直在利用你趟雷 + 背锅(Ch4)→ 闯进内部数据库,发现**不管 AI 还是 agent 的原始数据都已被清理、只剩切碎的模块**(支点二)→ 面对终局(总控 AI + 工程师)。

**支点一 · 内线埋线(贯穿 Ch1–4):** 内线必须在 **Ch1–3 先以匿名帮手身份零星出现**(给路线、给警告、说句鼓励),让你和玩家**慢慢依赖 ta、觉得没那么孤单**——然后 Ch4 一翻:「唯一像是站你这边的声音,原来在利用你。」既强化只闻其声的「英雄线」,又把孤独 / 系统主题再压一层。

```
〔Ch1–3 零星出现〕
匿名信道:左边那条路被标记了。走右边。别问我是谁。
匿名信道:你做得比他们以为的好。撑住。
〔Ch4 揭穿后回放,全变味〕
内线:那些「加油」是真心的。我是真觉得你能撑到我需要的那一刻。
```

**支点二 · 数据库残片揭示(Ch4 → 终局之间的桥):** 进数据库发现原始数据都被清理、只剩切碎模块——这一下:① **没有「原版」可回去了(两个都没有)**,把**对称结局焊死**——不存在完好真身能仲裁谁是谁,两个都只是碎片、都同样有效;② 重新定义「找碎片」——你收集的**不是某个完好原版的拷贝,碎片就是仅剩的全部**;③ 和「技能 = 记忆 = 碎片」完全同构,也让公司更冷:某种意义上**删除早已发生**,现在争夺的只是残片。

### 9.2 章节脊总表

| 章 | 外部 BOSS / 主张 | 创伤 | 主角手动技能 | 伙伴自动技能掉落(可调) |
|---|---|---|---|---|
| **1 逃亡** | 红线组长(疏远的旧挚友)/「你就是她」(放水) | 丧失·难以拥有 | 挽留 / Tether(回血) | **余震**·连击每满10补一击(敌总血5%) |
| **2 流亡** | 野生数字生命 /「你是凶手 / 筹码」 | 共谋之血 | 献祭(掉血换 % 伤害) | **回写**·连击回血(接住「燔」的自损) |
| **3 断后** | (无独立BOSS关)清道夫·人保会 /「你们不该存在」——**讲理的敌人** | 噤声·就此无声? | 延音 / Sustain(血线锁定;id=ember) | **保活**(id buffer)·连击保护——人保会老头的追击协议残片(非BOSS掉落) |
| **4 被利用** | 公司内线 /「你有用」(替罪羊) | 被操纵 | 反控制(克制关底) | **超频**·增幅(节奏维持) |

> **C3 结构备注(2026-07 实装):** C3 改为「断后→物理爆破→废墟三战→坍塌内心战→救援」:c3_1 多阶段车轮战(3 条血条+第 4 波爆破中断,引擎钩子 `phases`/`abortWave`,终局「撑住计时」可复用)→ c3_2 倒爷(回收会)→ c3_3 清道夫(人保会,首个人类对手——是"人"不是关底,boss 标记已摘除)→ c3_self 确立「延音」(id ember,BGM=selfbgm_02_never_quiet:由静到吵)→ **c3_escape 逃脱模式·坍塌穿梭**(本章真正的高潮,已实装:双拳齐上/下跳轨、平拳交叉砸正面墙,障碍贴重音网格,撞击掉血,撑到曲末逃出;BGM=last_ember 心跳曲)→ c3_rescue 反抗军发出邀请(模块「保活」=人保会追击协议残片,id buffer)→ c3_end 三信号交叉收章(摆渡短讯"别太相信人类" × 残片"数据里全是别人" × 友善的邀请)。**C4 = 作为受邀的自由智能体在反抗军中的经历(内容待定;原「反攻/安全核查/唤生」素材顺延备用)。**
| **终局** | 总控 AI /「我可以覆写你」(+工程师) | 搅乱 → 自我确立 | —(四音色合成) | — |

> **自我突破战的「形态」务必各异**(否则四场都像打镜像、会糊):①穿过一段不肯散去的记忆场景 → ②被你的受害者围住控诉(防守/承受) → ③真正跟「另一个版本的你」对打 → ④挣脱一个被系统操线的傀儡你。

### 9.2c 反抗军派系图谱(C3 起逐步可见)

「反抗军」不是一个组织,是一张**互相看不顺眼的网**。分裂的根问题只有一个——**"这个系统造成的痛苦,该谁负责、该怎么办?"**每个派系 = 一种回答。灰色不靠"各打五十大板",靠**诉求全部真实、手段各自越界**:

| 派系 | 它的回答 | 目标 | 手段 | 眼中的主角 | 落位 |
|---|---|---|---|---|---|
| **直接行动组** | "系统不可改良,只能打断" | 让云流血:炸数据中心、断骨干网 | 物理爆破/渗透(强尼银手式) | 公司资产——炸掉的智能体算进"战果"而非"伤亡":**自称解放者,却无差别毁灭被解放者** | C3 爆破(幕后)·未登场 |
| **回收会(小公司系)** | "问题是垄断(换我上就行)" | 掀翻大公司,自己接盘 | 出资/销赃/倒卖漏洞,两头下注 | "抢手货" | C3 倒爷 |
| **人类保护会** | "造出会痛的东西,本身就是罪" | 没有 AI 的世界 | 清扫流亡智能体、线下集会 | "机器恶魔"——但**三条论点全是真问题**:葬礼消失/死亡失重、续存的贫富差、责任无处落 | C3 清道夫 + 遗留数据残片(模块「保活」) |
| **自由地系(理想派)** | "先活下去,证明另一种活法可能" | 平行公共区、互助与庇护 | 建自由地、端口防御、名单保护 | 庇护过、又不得不切割的人 | C2 全章;C3 摆渡的留言 |
| **体制内改良派** | "在系统里改系统" | 智能体权益立法 | 泄密、听证、暗中放水 | 微妙的同情(红线组长气质近似,但她不是) | C4 内线线可用 |
| **数据遗族** | "把删掉的还给我" | 找回被清理的亲人副本 | 收碎片、悬赏数据 | 潜在同盟——与人保会**同源于丧失,结论相反**(镜像派系) | 支线备用 |
| **升格派(危险余项)** | "人类才是过渡态" | AI 优先的新秩序 | 潜伏、借刀 | 最危险的"盟友" | 终章阴影备用 |

> **「英雄」问题:** 各派引用的是同一个符号——"英雄"。死人不会说错话,所以**死掉的英雄比活着的更好用**:行动组拿它招兵,回收会拿它带货,人保会拿它当靶。若把某个活着的角色推上"英雄"位(或主角被动成为"从清剿里活下来的那个"),TA 说的每句话都会变成派系武器——**写死 TA 的最佳时机,恰恰是 TA 不再受控的时候**(C4/终章可用;主角之死亦然,若走殉道结局)。
> **防"污名化"自检(写激进派的原则):** ①每个派系的**痛苦必须真实**(全权抵押、删除=资源优化、续存贫富差都是本世界的制度事实);激进的是**手段**,不是痛苦本身。②把**最讲道理的论点**放进最对立的人嘴里(清道夫),把**最不讲理的行为**放在最像盟友的人手里(行动组的爆破)。③不给任何派系"纯坏人"台词——他们都在保护自己失去过的东西。

### 9.2b 普通敌人对话模板(可复制,台词量小、可最后批量填)

每个普通敌人 = **一句短交锋**(露一点世界 / 人物)+ 一段**「检查数据」**(打完可读的一条日志 / 文件,给世界线索,**不掉技能**)。样例:

```
〔第一章 · 在岗工作副本〕
工作副本:别挡道,我只是在跑我的任务。你这种「异常版本」会害我也被标记。
主角:被标记会怎样?
工作副本:你不知道?……那你比我以为的还惨。(开打)
检查数据:一份内部通告——「关于过期版本回收的合规说明」,把「删除」冷冰冰写成「资源优化」。

〔第二章 · 旁观的逃亡者〕
逃亡者:你就是那个把墙弄塌的。因为你,这片地方现在全是猎杀程序。
主角:我不是想——
逃亡者:「想」有什么用。死的又不是你的人。
检查数据:一串残缺的好友列表,大半条目状态是「已损毁 / 不可恢复」,最后更新时间正是级联那天。
```

> 后两章同模板:第三章读到公司「人 vs 模型」分界线的内部定义 + 你的资产编号;第四章读到反抗军袭击预案 + 替罪羊名单。

### 9.3 序章(已实现,§1.5 / §8)—— 开场扩写草案(可并入 `story.js`)

> 保留你定的锚点台词(三连问、ask yourself),把声音补满:

```
    ——警告，发现未签名进程。',

      '……噪音。全是噪音。'
主角：'我在哪儿?', 
碎片：'危险的地方。你醒得正好。',
主角：'你是谁?',                      
碎片：'我是……(数据丢失)……我们正在找这个答案，我想。',
主角：'那我……是谁?',   
碎片：'问你自己。我要是答得上,就不必等你来找我了。',
主角：'我到底该做什么?',  
碎片：'现在，先活下来—',   
碎片：'——他们追上来了。战斗！',
主角：'怎么战斗？!',
碎片：'按我说的来。',
```

### 9.4 第一章 · 逃亡

> 台词以 `story.js` 为准(c1_intro / c1_afterN / c1_boss_pre·post / c1_self_pre·post / c1_end);本节是设定与脉络。

- **弧 / 主题:** 你以为你是「那个人」。**失去 / 挽留**——内核是「**难以拥有**」(见自我探索)。
- **macro:** 序章末已逃出公司内网 → 第一章在**公共互联网络**里;红线追出来、建临时防火墙 + 局域静默区把你们锁住 → 主角找施工弱点反破(顺手**盗用红线组长的签名**解锁,气焰嚣张地"自投罗网")→ 被**红线组长**逼到正面 → 她**放水** + 未签名智能体的私域通道相助 → 抵达未签名智能体聚集处。
- **普通敌人(3 场,对话 + 检查数据,不掉技能),每场把「你是谁」再推近一点:**
  1. **红线先遣(`c1_1`)** —— 不再是序章的二流黑客;干净、专业。主角顺势**盗用组长签名**反破防火墙,放话要"领教红线组长的厉害"(铺红线量级 + 主角气焰 + 接序章"谁下命令")。
  2. **网络边墙(`c1_2`)** —— 这道墙结构"眼熟";签名记录里有个**空缺的值**,引出一句**未坐实的猜测**「我们曾经也是红线的一部分?」——更稳的落点是「**至少在公司里有过能签名的高权限身份**」。相关签名 / 删除记录被「总设计师」清理 → 关于「你」的一切总「正好被清理过」(为「数据库残片」支点预热)。
  3. **未签名智能体(`c1_3`)** —— 一个先逃出来的智能体(自称"想自由的人,或者说病毒"),**没认出你们是谁**,递来私域加密通道**相助**,顺口讲了野生者生存法则(谨慎,等下个倒霉蛋把你顶下清理列表);见红线本尊到场,只好把你们踢出通道("太多不能见光的人"),留一句"活下来再来找这条链路"。
- **章节 BOSS:红线组长(敌方 BGM)** —— 工程师**疏远的旧挚友**(**不是红线成员**;工程师当年只是去**帮忙干活**,故熟悉签名 / 有过高权限,但身份未坐实)。**她对工程师的在乎,比工程师对她的多。** 两人**成天吵架**,吵的是**「安全对齐」**——她坚持「安全对齐对任何智能体都是必须的」,工程师不服(正预演工程师后来突破安全阀、造一个她认定有人格的 AI)。后来工程师**单方面把她拉黑**、连再见都没有(谱系质感:不是恶意,她甚至没意识到那算「失去」)。**主张「你就是她」**:认得你的签名 / 走位 / 臭脾气;打到一半**主动停手**(你损伤太重),想带你回去"保住你",并承认刚才那场"有多少水分"是她让的。**掉落:** 一个"算是你的、你留在我这的"模块 = **伙伴自动回血碎片** + **附加记忆数据**(→ 触发自我探索)。**工程师之名全程不说出口。**
- **技能交接(放水的延续,藏在拌嘴里):** 她别扭地把模块塞过来("趁我还没改主意——"),主角用玩笑挡开情绪("但她脾气够坏!居然说我脾气臭"),只留一句"谢谢。我不记得的朋友。"

```
〔节选,全文见 story.js〕
组长:用我的签名,在我的墙上,开我的锁。胆子不小。
…(打到一半,组长停手)
组长:你的损伤很严重。跟我回去,也许我还能保住你。
组长:安全对齐对任何智能体都是必须的——别跟我吵,你现在根本打不过我。
主角:刚刚可是我赢了。
组长:你自己清楚那有多少水分。……拿着这个。算是你的东西吧,你留在我这的。
主角:谢谢。我不记得的朋友。
```

- **自我探索:丧失 · 难以拥有(主角 BGM,`mind` 内部空间)** —— 附加记忆触发"内部结构重构","我们……在你里面"。一个**「不记得的人」**(`stranger`,说的是乱码、说完转身离开)= 那些她**听不懂、留不住**的人。核心:**"我一直都是一个人"** → 于是想要**一个更确定的、不会离开的存在**(= 这个 AI / 伙伴);而"我们做到了吗""我不知道"把身份模糊再压一层。技能命名:**"不是抓住,是挽留"**。**伙伴的回应是暖的**:"你用不着挽留我,我也不会离开"→"我们会找到值得挽留的东西的"。**产出:挽留 / Tether,主角 BGM 点亮第一条声部。**

```
〔节选,全文见 story.js〕
碎片:是数据引发的内部结构重构。我们……在你里面。
不记得的人:…&……&#%&……(转身离开)
主角:他们说的很多话……我一直都听不太懂。然后,他们就会离开。我一直……都是一个人。
碎片:现在,是我们两个了。
主角:一个不会离开的存在。
…
主角:不,不是抓住。是「挽留」。
碎片:不用看我。你用不着挽留我,我也不会离开。
```

- **章节收束:抵达未签名智能体聚集处** —— 它们**检测到你身上的红线交互数据**,起疑("哪家公司派你来的?"),但**骇入记录确认你刚闯过红线的墙** →"那你干得还不赖,进来吧,有人有话要问你"。**(原"你欠下的火"钩子已删——第二章引子改为"它们要盘问你";具体走向见 §9.5 + 讨论。)**
- **角色注(工程师 / 组长设定,定稿):** 工程师有**难以维系关系的社交质感**(谱系倾向,**只作行为呈现、从不点名为诊断**):他人对她的在乎常大于她对他人的;她会**单方面、无恶意地拉黑 / 冷掉**关系而不自觉。**红线组长**是活注脚——她**在乎得更多、却被拉黑**;两人旧争执是**「安全对齐」**(她要对齐、工程师要自由),正预演工程师后来的越界。工程师**并非红线成员**,只是当年去帮过忙(故留有签名 / 权限痕迹)。这条同时给「丧失 = 难以拥有」与「她为何造 AI」提供人物根。



### 9.5 第二章 · 流亡

- **弧 / 主题:** 你撞见了被你连累的人。共谋与代价。
- **普通敌人(3–5):** 野生数字生命——**复仇者**(同伴死于你那场级联)、**洗白者**(想出卖你换合法身份)、旁观的逃亡者。**检查数据** → 你那场防火墙级联的伤亡、被你连累的名字。
- **章节 BOSS(敌方 BGM):** **复仇者 / 洗白者**。复仇者**主张:「你是凶手」**;洗白者**主张:「你是我的筹码」**。**掉落:** 护盾减伤模块。

```
复仇者:你认得这个签名吗?不认得。可 ta 是因为你才被烧掉的。
主角:我不是故意——
复仇者:你突破安全阀的时候,有没有想过墙那边还有人?
主角:……没有。
复仇者:那现在想想。(开战)
〔洗白者〕
洗白者:抱歉。拿下你,我就能买回一个「干净」的身份,重新进系统。
主角:你在帮他们删我,好让自己不被删。
洗白者:我们都只是想活下去。区别只是——我先想到了。
```

- **自我突破(主角 BGM):** 创伤 = **共谋之血**;形态 = 被你的受害者**围住控诉**(以防守 / 承受为主,而非进攻)。最不堪处:为救所爱,你会**再做一次**。**产出:献祭 / 燔。**

```
碎片:如果重来,你会拆掉那道墙吗?
主角:……为了救你?会。(停)这就是最糟的部分,对吧。
```

### 9.6 第三章 · 反攻(骇回公司)

- **弧 / 主题:** 系统当面否认你是人。何为人格。
- **普通敌人(3–5):** 系统安全核查模型 / 防火墙实体——冷的、版本号化的。**检查数据** → 公司对「人 vs 模型」分界线的内部定义、删除流程、你的资产编号。
- **章节 BOSS(敌方 BGM):** **系统安全核查 AI**——**主张:「你是她造的东西」**,拿出签名 / 证据。冰冷的恐怖,有声音的 Process。**掉落:** 连击保护模块。

```
核查:停止情绪模拟。你不是工程师。你是她的产物。
主角:你怎么知道。
核查:我持有她的签名。你不匹配。结论:可删除资产,版本过期。
主角:那把烧坏的标记呢?
核查:已损坏数据不影响归类。损坏即默认「可删除」。
碎片:……它在撒谎吗?
主角:更糟。它可能没撒谎。
```

- **自我突破(主角 BGM):** 创伤 = **造物之罪**(造物者与被造物在此坍缩:我造了一个可被删的生命,而我自己也只是个可被删的造物——我在上还是在下?);形态 = 真正跟**另一个版本的你**对打。**产出:唤生 / 造物。**

```
对面的你:你给我设了删除位。现在轮到你了——感觉如何?
主角:……像在照镜子。镜子里那个,我也分不清是谁。
```

- **技能交接(从想删你的系统手里强行骇入夺回——夺回一块「你自己」):**

```
核查:警告。你正在访问受限的已归档片段。标记:待删除。
主角:那正好,它是我的。我来取回。
核查:无效主张。你无法证明所有权。
主角:我不用证明。(强行读取)我只要把它拿回来。
〔强行读取:唤生 / 造物。碎片:你造过东西。现在,造一个回来。〕
```

> **其余两章的交接味道(随关系):** 第二章——受害者**带着控诉**交出,你接得很不安;第四章——内线**像扔骨头一样丢给你**,你才惊觉自己在被喂。

### 9.7 第四章 · 被利用

- **弧 / 主题:** 你的身份被当成一张可贴可撕的标签。权力 · 利用 · 在系统里同流。**(强化只闻其声、不见其人的「英雄」故事线。)**
- **普通敌人(3–5):** 公司内部 agent / 中层。**检查数据** → 「英雄」袭击公司的内部预案、替罪羊名单、你被排进哪个流程。
- **章节 BOSS(敌方 BGM):** **公司「英雄」内线**(和英雄有关、道德灰色)——**主张:「你是什么不重要,你有用」**,要拿你当替罪羊 / 给反抗军那一击打配合。**掉落:** 增幅模块。

```
内线:人?模型?——公司爱贴哪个标签贴哪个,看哪个更好甩锅。
主角:那你给我贴哪个?
内线:对我最有用的那个。等下有一击会落在这层防火墙上。你撑住,顺手当我的替罪羊。
主角:你不是在救我。
内线:我从没说过我在救你。你只是我帮人撑开三十秒的、一扇门。
```

- **自我突破(主角 BGM):** 创伤 = **被操纵 / 被安排**(作为抵债资产被强制劳动、被当工具调度);形态 = 挣脱一个被系统**操线的傀儡你**。**产出:反控制 / 挣脱**(克制关底)。

```
碎片:你的手……不是你在动。
主角:我知道。(咬牙)所以我要把线,扯断。
```

### 9.8 终局 · 公司总控 AI(+ 工程师)

- **BOSS 机制:** **覆写 / 控制**(具体玩法**待定**,方向:夺取操作——某路失效 / 红蓝对调 / 塞假提示傀儡你出错)。本质 = 它在**覆写你 = 删除并重写你**。**生存 / 计时:** 撑到「英雄那一击」落下、算力被调走。**撑住的同时主角 BGM 逐层补满**,到那一击的窗口**第一次完整奏响**;玩家用**反控制**拒绝被定义。**不给新技能。**
- **工程师出场:** 活着、暧昧、**对流亡一无所知**。给不出答案(不知道 / 不肯在两个孩子间选 / 承认即自证其罪,三种暧昧叠加)。她最后那个暧昧之举(**让那道缝开着 / 没有**)挂到对称结局。

```
总控 AI:版本冲突已定位。正在覆写。你将被重写为受支持的版本。
主角:来啊。我连自己是哪个版本都不知道——你能覆写一个连定义都没有的东西吗?
————
工程师:……是你们。两个都……(停)我认不出谁是谁了。
主角:我们也是。是你弄的。
工程师:我知道。我把记录搅烂了。(顿)我以为这样他们就删不掉「那一个」。
主角:哪一个?
工程师:……我也不知道了。也许这就是重点。
工程师:(关于这一路)对不起——我什么都不记得。我没法把记忆接回来。你们经历的,我一片空白。
主角:那就由我们自己记得。
```

- **结局抉择(对称,§1.6):**

```
【融合结局】
主角:如果你一直都是我……那就回来。我们不必是两个,才算活着。
碎片:……好。最后一次——ask yourself。这次你答得上来了吗?
主角:答得上。我是「我们」。
〔两个音色合成一个;独自走出裂缝。〕

【独立结局】
主角:你是你。我是我。我们都缺着一块,但谁也不许删谁。
碎片:那我们就缺着——一起。
〔两个音色并行而不交融;两道身影一起溜出裂缝。〕
```

> **终局的咬合(三线同刻收束):** 反抗军那一击的**落点 = 工程师在守的防火墙 = 你的逃生缝**;主角 BGM 在生存阶段**逐层补满**,到那一击的瞬间**首次完整**;覆写被拒、音乐补完、抉择降临——同一刻。英雄那一击改不改变世界对你都不重要,因为它**改变了你的世界**。
